<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Linguibuddy.ViewModels"
             xmlns:loc="clr-namespace:LocalizationResourceManager.Maui;assembly=LocalizationResourceManager.Maui"
             xmlns:models="clr-namespace:Linguibuddy.Models"
             x:Class="Linguibuddy.Views.AudioQuizPage"
             x:DataType="vm:AudioQuizViewModel"
             Title="{loc:Translate AudioRecognize}">
    <Grid>

        <VerticalStackLayout IsVisible="{Binding IsFinished}"
                             VerticalOptions="Center"
                             HorizontalOptions="Center"
                             Spacing="25">

            <Label Text="{loc:Translate Congratulations}"
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light={StaticResource PrimaryDarkText}, Dark={StaticResource White}}"
                   HorizontalOptions="Center" />

            <Label Text="{loc:Translate LearningFinished}"
                   FontSize="16"
                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                   HorizontalOptions="Center" />

            <Label FontSize="20"
                   FontAttributes="Bold"
                   HorizontalOptions="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{loc:Translate CorrectAnswersCount}" />
                        <Span Text=" " />
                        <Span Text="{Binding Score}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>

            <Label FontSize="16"
                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                   HorizontalOptions="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="{loc:Translate PointsEarnedCount}" />
                        <Span Text=" +" />
                        <Span Text="{Binding PointsEarned}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>

            <Button Text="{loc:Translate BackToList}"
                    Command="{Binding GoBackCommand}"
                    Margin="0,20,0,0"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                    TextColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource PrimaryDarkText}}"
                    WidthRequest="200" />
        </VerticalStackLayout>

        <Grid RowDefinitions="Auto, Auto, *, Auto" Padding="20" RowSpacing="20"
              IsVisible="{Binding IsLearning}">

            <VerticalStackLayout Grid.Row="0" Spacing="15" HorizontalOptions="Center">
                <Label Text="{loc:Translate AudioQuizDesc}"
                       FontSize="18"
                       TextColor="Gray"
                       HorizontalOptions="Center" />

                <Frame CornerRadius="40"
                       HeightRequest="80"
                       WidthRequest="80"
                       BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                       HasShadow="True"
                       Padding="0">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding PlayAudioCommand}" />
                    </Frame.GestureRecognizers>

                    <ImageButton
                        Source="{AppThemeBinding Light=text_to_speech_100dp_dark.png, Dark=text_to_speech_100dp_light.png}"
                        Command="{Binding PlayAudioCommand}"
                        BackgroundColor="Transparent"
                        Padding="0"
                        HeightRequest="30"
                        WidthRequest="30" />
                </Frame>

                <Label Text="{loc:Translate AudioClick}" FontSize="12" HorizontalOptions="Center" TextColor="Gray" />

                <Label Text="{Binding TargetWord.Phonetic, StringFormat='/{0}/'}"
                       FontSize="24"
                       FontAttributes="Italic"
                       TextColor="{StaticResource Primary}"
                       HorizontalOptions="Center">
                    <Label.Triggers>
                        <DataTrigger TargetType="Label" Binding="{Binding TargetWord.Phonetic}" Value="">
                            <Setter Property="IsVisible" Value="False" />
                        </DataTrigger>
                        <DataTrigger TargetType="Label" Binding="{Binding TargetWord.Phonetic}" Value="{x:Null}">
                            <Setter Property="IsVisible" Value="False" />
                        </DataTrigger>
                    </Label.Triggers>
                </Label>
            </VerticalStackLayout>

            <Label Grid.Row="1"
                   Text="{Binding FeedbackMessage}"
                   TextColor="{Binding FeedbackColor}"
                   FontSize="18"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center" />

            <Grid Grid.Row="2"
                  BindableLayout.ItemsSource="{Binding Options}"
                  ColumnDefinitions="*,*"
                  RowDefinitions="*,*"
                  ColumnSpacing="15"
                  RowSpacing="15">

                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="models:QuizOption" />
                </BindableLayout.ItemTemplate>
            </Grid>

            <CollectionView Grid.Row="2" ItemsSource="{Binding Options}" SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="2" HorizontalItemSpacing="10"
                                     VerticalItemSpacing="10" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:QuizOption">
                        <Button Text="{Binding Word.Word}"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:AudioQuizViewModel}}, Path=SelectAnswerCommand}"
                                CommandParameter="{Binding .}"
                                BackgroundColor="{Binding BackgroundColor}"
                                TextColor="White"
                                HeightRequest="80"
                                IsEnabled="{Binding IsEnabled}"
                                FontSize="16"
                                CornerRadius="10" />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Grid.Row="3"
                    Text="{loc:Translate Next}"
                    Command="{Binding NextQuestionCommand}"
                    IsVisible="{Binding IsAnswered}"
                    BackgroundColor="{StaticResource Primary}"
                    Margin="0,0,0,20" />

            <ActivityIndicator Grid.RowSpan="4"
                               IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
        </Grid>
    </Grid>
</ContentPage>